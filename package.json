{
  "name": "sender-rotate",
  "version": "1.0.0",
  "scripts": {
    "start": "live-server --host=127.0.0.1 --port=5500",
    "backend": "node backend.js"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "ethers": "^5.7.2"
  }
}